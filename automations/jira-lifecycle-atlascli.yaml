name: jira-lifecycle-atlascli
pipeline:
  - given:
      trigger: github
      repos:
        - mongodb-labs/automata
        # - mongodb/mongodb-atlas-cli
        # - mongodb/atlas-github-action
        # - mongodb-labs/cobra2snooty
    when:
      - event: pull_request
        action: opened
        actor: "dependabot[bot]"
    then:
      - jira.create_issue:
          id: ticket
          issue_type: Story
          project: CLOUDP
          component: AtlasCLI
          custom_fields:
            customfield_12751: [{id: "22223"}]
            customfield_10257: {value: "Not Needed"}
          summary: "[{payload.repository.name}] {payload.pull_request.title}"
      - github.post_comment:
          owner: "{payload.repository.owner.login}"
          repo: "{payload.repository.name}"
          number: "{payload.pull_request.number}"
          body: "Jira ticket: {ticket.url}"
      - github.add_label:
          owner: "{payload.repository.owner.login}"
          repo: "{payload.repository.name}"
          number: "{payload.pull_request.number}"
          label: auto_close_jira

  # Manual trigger for testing: add the `create_jira` label to any PR in this repo
  # to fire ticket creation. The label is removed after the ticket is posted.
  - given:
      trigger: github
      repos:
        - mongodb-labs/automata
    when:
      - event: pull_request
        action: labeled
        label: create_jira
    then:
      - jira.create_issue:
          id: ticket
          issue_type: Story
          project: CLOUDP
          component: AtlasCLI
          custom_fields:
            customfield_12751: [{id: "22223"}]
            customfield_10257: {value: "Not Needed"}
          summary: "[{payload.repository.name}] {payload.pull_request.title}"
      - github.post_comment:
          owner: "{payload.repository.owner.login}"
          repo: "{payload.repository.name}"
          number: "{payload.pull_request.number}"
          body: "Jira ticket: {ticket.url}"
      - github.add_label:
          owner: "{payload.repository.owner.login}"
          repo: "{payload.repository.name}"
          number: "{payload.pull_request.number}"
          label: auto_close_jira
      - github.remove_label:
          owner: "{payload.repository.owner.login}"
          repo: "{payload.repository.name}"
          number: "{payload.pull_request.number}"
          label: create_jira
